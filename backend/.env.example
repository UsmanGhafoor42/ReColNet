# Local development
DEBUG=true
DATABASE_URL=sqlite+aiosqlite:///./recolnet.db
CORS_ORIGINS=["http://localhost:3000"]
COLORIZE_ENGINE=auto
DEOLDIFY_ARTISTIC=true
DEOLDIFY_RENDER_FACTOR=35

# Railway production (set in Railway dashboard, not committed)
# CORS_ORIGINS=["https://re-col-net.vercel.app","http://localhost:3000"]
# COLORIZE_ENGINE=opencv
# DATA_DIR=/data
